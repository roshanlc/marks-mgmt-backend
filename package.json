{
  "name": "internal-examination-records-backend",
  "version": "0.0.1",
  "description": "The backend for internal examination records management system.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js",
    "lint": "eslint src --ext js --report-unused-disable-directives --max-warnings 0",
    "lint-fix": "eslint src --fix --ext js --report-unused-disable-directives --max-warnings 0",
    "start": "NODE_ENV=production node src/index.js"
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": [
    "marks-management"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "nodemon": "^2.0.22"
  }
}